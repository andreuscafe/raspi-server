{
  "name": "server-test",
  "version": "1.0.0",
  "description": "simple server test for port forwarding",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "andreuscafe",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
